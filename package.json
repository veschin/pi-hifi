{
  "name": "pi-hifi",
  "version": "0.1.0",
  "description": "pi-hifi: verification-centric inference-time reasoning extension for Pi - triage-gated composition, stack-agnostic sandboxed execution, GVR loops, external verifier, causal candidate selection (the 'apodex' validate+select step), evidence-disciplined answers",
  "type": "module",
  "private": true,
  "pi": {
    "extensions": [
      "./index.ts"
    ]
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "eval": "tsx eval/run-eval.ts",
    "eval:smoke": "tsx eval/run-eval.ts --smoke"
  },
  "devDependencies": {
    "@earendil-works/pi-ai": "^0.79.1",
    "@earendil-works/pi-coding-agent": "0.79.1",
    "@types/node": "^24.13.2",
    "tsx": "^4.22.4",
    "typescript": "^5.7.2"
  }
}
